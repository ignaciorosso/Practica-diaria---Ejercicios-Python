# Crear una lista por asignación con 5 enteros. Eliminar el primero, el tercero y el último de la lista.

lista = [57, 34, 2, 45, 45]
print(lista)
lista.pop(0)
#print(lista.pop(0)) # Mostramos el elemento a eliminar
print(lista)
lista.pop(1)
print(lista)
lista.pop(2)
print(lista)